import { createTheme } from '@mui/material/styles';

const mainTheme = createTheme({
  palette: {
    primary: {
      main: '#2e1aad',
      contrastText: 'red',
    },
  },
});

export default mainTheme;
